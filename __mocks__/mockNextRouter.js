import Router from 'next/router'

Router.router = {
  pathname: '/',
  route: '/',
  asPath: '/',
  query: {},
  push: () => {},
  replace: () => {},
  prefetch: () => new Promise((resolve, reject) => {}),
}
